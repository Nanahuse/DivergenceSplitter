"""Windows desktop UI for DivergenceSplitter.

This package owns the Dear PyGui screens and texture management. It uses the
``divergencesplitter-runtime`` public API and does not re-implement capture,
processing, or Bridge communication.

The package boundary only is established at this stage; screen and session
control land in a later milestone. Package metadata is the version authority.
"""
