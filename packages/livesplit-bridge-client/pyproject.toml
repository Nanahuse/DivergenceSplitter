[project]
name = "livesplit-bridge-client"
version = "0.1.0"
description = "Typed Python client for LiveSplit.Bridge"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "protobuf>=6.33.5,<7",
    "pyzmq>=26,<28",
]

[dependency-groups]
dev = [
    "grpcio-tools>=1.68,<2",
    "pytest>=9.1.1",
    "ruff",
    "ty",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/livesplit_bridge_client", "src/livesplit"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["src/livesplit/bridge/v1/*_pb2.*"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "PYI"]

[tool.ty.rules]
conflicting-metaclass = "ignore"
